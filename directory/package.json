{
  "name": "artifex-directory",
  "version": "1.0.0",
  "description": "Standalone public directory for Artifex instance discovery — registry only, no content relay",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^5.0.0"
  }
}
